# WBS-16-WP-006 Controlled Work Package

## Objective

Implement the decided Agent, model/provider, Tool and Context/Memory security architecture as immutable, provider-neutral contracts and inert reference boundaries that preserve Human-primary authority, least privilege, Zero Trust, current authorization, fail-closed behavior, provenance, minimization, sovereignty and African data-residency constraints.

## Delivered scope

- immutable Agent definition, primitive, symbolic resource-ceiling and generated-code quarantine contracts;
- empty, versioned Agent primitive, model/provider/data-combination, Tool eligibility and cross-context exception registries;
- unassigned interfaces covering every decided WP-006 institutional authority class;
- zero-capability Agent Factory validation with no Agent lifecycle or runtime;
- eligibility-first, no-route Model Gateway with no adapter or provider call;
- exact four-class Tool taxonomy, high-risk predicates, current authorization checks and a no-invocation Tool Gateway;
- synthetic prompt-injection/Tool-misuse checks that prevent untrusted data from acquiring authority;
- eight Context classes, exact exception metadata, current/expiry/revocation evaluation and a deny-all retrieval/transfer boundary;
- minimized, non-authoritative provenance/security signal contracts;
- standard-library deterministic tests and cumulative local verification.

## Exclusions

No Agent or generated executable code is created or executed. No provider/model/route, Tool/invocation, Context exception/transfer, credential, endpoint, network access, external system, governed production data, operational authority assignment, numeric threshold, dependency, infrastructure, deployment, WP-007 scope, accreditation, acceptance or go-live is included.

## Stop conditions

Stop the affected scope and return for Human decision if implementation requires a concrete authority holder, numeric ceiling/threshold, provider/model/deployment, Tool, context pair, destination, governed data, generated code, network/external capability, new dependency/infrastructure, a change to WP-001–WP-005, or any expansion beyond decision `...-024`. The fallback is deny/no capability.

## Completion criteria

Completion requires all 24 criteria in the decided Human Review Decision Pack, including empty controlled registries, zero executable paths, exact taxonomies, current authorization, comprehensive synthetic negative tests, cumulative WP-001–WP-005 regression, strict typing/lint/format/compilation, template verification, traceability and evidence that makes no operational assurance claim.
